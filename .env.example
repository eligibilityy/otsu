# Client credentials — required for `npm run test:guest`
OSU_CLIENT_ID=
OSU_CLIENT_SECRET=

# User access token — required for `npm run test:authenticated`
# Obtain via `npm run example:auth` or your own OAuth flow.
OSU_ACCESS_TOKEN=
